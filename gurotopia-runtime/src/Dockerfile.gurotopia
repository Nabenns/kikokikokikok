FROM ubuntu:24.04

RUN apt-get update && apt-get install -y --no-install-recommends \
    build-essential libssl-dev libmariadb-dev \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /app
COPY . .

RUN make -j$(nproc)

# ENet UDP 17091, HTTPS TCP 443
EXPOSE 17091/udp 443/tcp

CMD ["/app/main.out"]
